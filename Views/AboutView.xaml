<UserControl x:Class="MVVM.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="20" VerticalAlignment="Center">
        <TextBlock Text="{Binding AppName}" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>
        <TextBlock Text="{Binding Version}" FontSize="16" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>
        <TextBlock Text="Разработано с использованием MVVM и WPF" 
                   FontSize="12" HorizontalAlignment="Center" 
                   Foreground="Gray" Margin="0,20,0,0"/>
    </StackPanel>
</UserControl>
