<Window x:Class="MVVM.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Телефонная книга" Height="500" Width="700">
    <DockPanel>
        <!-- Верхнее меню навигации -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" 
                    Background="LightGray">
            <Button Content="Контакты" 
                    Command="{Binding ShowContactsCommand}"
                    Margin="5" Padding="10,2"/>
            <Button Content="О программе" 
                    Command="{Binding ShowAboutCommand}"
                    Margin="5" Padding="10,2"/>
        </StackPanel>

        <!-- Область контента для отображения текущего экрана -->
        <ContentControl Content="{Binding NavigationService.CurrentViewModel}" Margin="10"/>
    </DockPanel>
</Window>